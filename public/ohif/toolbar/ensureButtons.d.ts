/**
 * Dynamically inserts the custom Scroll button and W/L Preset dropdown
 * into the standard OHIF toolbar primary section.
 */
export declare function ensureButtonsInToolbar(toolbarService: any): void;
